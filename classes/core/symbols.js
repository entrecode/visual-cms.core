const idSymbol = Symbol('id');
const contentSymbol = Symbol('content');
const supportedContentSymbol = Symbol('supportedContent');
const settingsSymbol = Symbol('settings');

module.exports = {
  idSymbol,
  contentSymbol,
  supportedContentSymbol,
  settingsSymbol,
};
